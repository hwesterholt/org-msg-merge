;; tcsv-.el  -*- lexical-binding: t; -*-

;; Require the emacs-lisp library being tested.
;; Example: (require 'org-msg-merge)

(require 'org-msg-merge)

;; Test things that *should* happen.
(ert-deftest )

(provide 'tcsv)
